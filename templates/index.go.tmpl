<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Currículos</title>
</head>
<body>
    <h1>Lista de Currículos</h1>
    <table>
        <thead>
            <tr>
                <th>Nome</th>
                <th>Email</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            {{range .}}
            <tr>
                <td>{{.Name}}</td>
                <td>{{.Email}}</td>
                <td><a href="/detail/{{.ID}}">Detalhes</a></td>
            </tr>
            {{end}}
        </tbody>
    </table>
    <a href="/register">Novo Currículo</a>
</body>
</html>